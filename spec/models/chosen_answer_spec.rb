require 'spec_helper'

describe ChosenAnswer do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ChosenAnswer.create!(@valid_attributes)
  end
end
